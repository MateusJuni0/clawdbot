# 2026-02-13 - Log de Operações

## Status do Projeto: Restaurante Avilez
- **Deploy:** Várias tentativas de deploy via Vercel CLI com o token `[REDACTED_VERCEL_TOKEN]`.
- **Problemas Técnicos:** 
    - Conflitos de dependências entre React 18 e `@react-three/fiber` v9/`@react-three/drei` v10.
    - Erro de compilação por falta de `react-icons` e `framer-motion`.
    - Erro de SSR (Server-Side Rendering) no Next.js ao tentar usar componentes do Framer Motion (`motion.h1`) sem `"use client"` ou em contexto inapropriado (corrigido adicionando `"use client"` e ajustando a estrutura).
    - Erro de TypeScript no `FluidBackground.tsx` (resolvido com Type Casting para `ShaderMaterial`).
- **Isolamento:** Pasta `cmtecnologia-web` limpa de arquivos experimentais para proteger o site principal. Projeto redirecionado para `projects/bairro-avillez-site`.
- **Vercel:** Projeto criado como `restaurante-avilez` no time `cm-tecnologia`.

## Crise de Tokens
- O usuário (Mateus) reportou gasto excessivo (~15 euros) em uma única sessão devido a loops de erro de build e leitura de logs extensos.
- **Ordem Direta:** "Para tudo, só responde oi".
- **Ação:** Interrompi todas as automações e entrei em modo de economia máxima.

## Pendências
- Validar se o último deploy (`https://restauranteteste.blog`) finalizou com sucesso.
- Aguardar autorização para novas ações.
